@using PizzaApp.Models.Domain
@model List<Pizza>

    <h2>Pizzas: </h2>
    @*This is a comment*@

    <ul>
    @for(int i =0; i< @Model.Count; i++)  @*with Model we are accessing the value of the model that was sent by the controller*@
    {
        <li>@Model.ElementAt(i).Name</li>
       @*  <li>@Model[i].Name</li> *@
    }
</ul>